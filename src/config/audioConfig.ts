/**
 * =========================================================================
 * 🎵【全局背景音乐 (BGM) 音频资产 —— 一键替换配置中心】
 * =========================================================================
 * 📍 文件路径：/src/config/audioConfig.ts
 *
 * 📌【如何替换为你自己的背景音乐音频文件】：
 * -------------------------------------------------------------------------
 * 步骤 1：准备你的音频文件，支持格式包括：.mp3、.wav、.ogg、.m4a、.aac 等主流格式。
 * 步骤 2：
 *   👉 方法 A（本地文件）：将音频文件放入 public/assets/audio/ 文件夹中，
 *      例如将文件命名为 my_wuxia_bgm.mp3，
 *      然后在下方 bgmUrl 中填入：'/assets/audio/my_wuxia_bgm.mp3'
 *   👉 方法 B（网络在线链接）：直接将在线音频链接填入下方 bgmUrl，
 *      例如：'https://your-domain.com/music/bgm.mp3'
 *
 * 步骤 3：保存代码即可！
 *
 * 🔊【自动播放与循环播放说明】：
 * - 只要一打开游戏就会自动播放，并且全局循环播放（Loop: true）。
 * - 针对现代浏览器（Chrome/Safari/Firefox/Edge）的自动播放策略，系统已内置“双重唤醒”机制：
 *   1. 页面加载完成时立即尝试无缝自动播放；
 *   2. 若浏览器初始拦截无交互音频，在玩家第一次点击屏幕/按键的瞬间无感自动唤醒并持续循环！
 * - 玩家也可以在游戏右上角随心切换「琴剑合奏」、「古琴音韵」、「金铁剑鸣」或「一键静音」。
 * - 若 bgmUrl 为空字符串 ''，系统将自动无缝回退到内置的战国五声古琴实时合成律动！
 * =========================================================================
 */

export interface GlobalBgmConfig {
  /**
   * 🌟【一键替换位置】：在此处填入你的全局背景音乐文件路径或在线音频链接
   * 示例：
   * bgmUrl: '/assets/audio/my_wuxia_bgm.mp3'  // 本地文件
   * bgmUrl: 'https://cdn.example.com/bgm.mp3' // 在线文件
   */
  bgmUrl: string;

  /**
   * 背景音乐音量大小：0.0 (静音) ~ 1.0 (最大音量)，推荐 0.4 ~ 0.6
   */
  volume: number;

  /**
   * 是否循环播放：true (无限循环) / false (单次播放)
   */
  loop: boolean;

  /**
   * 是否在打开游戏时立即自动播放
   */
  autoPlay: boolean;
}

export const GLOBAL_BGM_CONFIG: GlobalBgmConfig = {
  // 👇👇👇【请在下方这一行直接替换你的音频文件路径或 URL】👇👇👇
  bgmUrl: '/assets/audio/spring_autumn_bgm.mp3',
  // 👆👆👆【请在上方这一行直接替换你的音频文件路径或 URL】👆👆👆

  volume: 0.5,
  loop: true,
  autoPlay: true,
};
